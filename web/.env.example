# Firebase Configuration
# Get these values from Firebase Console > Project Settings > General > Your apps
# After registering a web app, copy the config values from the Firebase SDK snippet

VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# Note: authDomain and storageBucket are auto-constructed from projectId
# Note: The API key is safe to commit and expose in client-side code.
# It's only used to identify your Firebase project, not for authentication.
